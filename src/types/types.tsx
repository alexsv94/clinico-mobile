export interface IAppContext {
	theme: IColorTheme;
}

export interface IColorTheme {
	contentColor: string;
	textColor: string;
	infoIconColor: string;
	iconColor: string;
	signatureColor: string;
	labelColor: string;
	accentColor: string;
	shadowColor: string;
} 